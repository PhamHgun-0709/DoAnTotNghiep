# ETL Module

Pipeline stages independent from runtime platform.

## Structure
- `ingestion`: Collect/load raw data.
- `transformation`: Clean, aggregate, and feature engineering.
- `serving`: Prepare data for API/dashboard consumption.
