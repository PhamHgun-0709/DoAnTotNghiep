# Button Component Library

Reusable button components for the frontend.

## Structure
- `common`: Generic buttons (primary, secondary, icon button).
- `campaign`: Buttons used in campaign management screens.
- `analytics`: Buttons for filters, date range, export actions.

Tip: keep button styles centralized to ensure consistent UI.
